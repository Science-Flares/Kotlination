package o

import kotlin.properties.ObservableProperty
@Deprecated("don't have attachment!")
class ObservableProperty {
    lateinit var observableProperty: ObservableProperty<*>
    init {
        observableProperty
    }
}